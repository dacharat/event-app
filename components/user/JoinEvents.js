import React from "react";
import { ScrollView, Text } from "react-native";

const JoinEvents = () => {
  return (
    <ScrollView>
      <Text>JoinEvents</Text>
    </ScrollView>
  );
};

export default JoinEvents;
